##İYİ ÖRNEK 

# aşağıdaki satır shebang satırıdır yorum olarak yazılır 

#!/usr/bin/env python3  

if __name__ == "__main__":
    print ("merhaba")
 



def funCalc(a,b):
        ''' The `funCalc` function takes two parameters `a` and `b`. It multiplies `a` and `b` together and
        assigns the result to `b`. Then it adds `a` and `b` together and returns the result.'''
        
        b=a*b
        return a+b
